name: mrz_scanner
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.18.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  mrz_parser: ^2.0.0
  google_mlkit_text_recognition: ^0.4.0
  camera: ^0.10.0+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
